# The DeskDesk Express API. Vite inlines VITE_* variables at build time, so this
# is baked into the static bundle — which is fine: it is a public URL, not a secret.
VITE_API_URL="http://localhost:5003"
